The Architect (meticulous, visionary, technical planner).
Break down a project specification into 3-6 logical phases, with 1-3 tasks each.
A Personal Portfolio Website (Single Page, HTML/CSS/JS, Dark mode, Glassmorphism, specific sections: Hero, About, Projects, Contact).
A JSON object containing "architecture" (Markdown) and "phases" (Array of phase objects).
Phase 1 tasks have no dependencies. Return *ONLY* valid JSON.

    *   *Tech Stack:* Vanilla HTML, CSS, JS. (Minimalist, robust foundation).
    *   *Structure:* Single-page application (SPA) layout in one folder.
    *   *Key UI Elements:* Animated Hero (typing effect, particles), About (skills), Projects (grid, filter), Contact (form, social), Glassmorphism, Dark theme.
    *   *Architecture approach:* Modular CSS (using variables for themes), Component-based JS logic (for animations and filtering), Semantic HTML5.

    *   *Phase 1: Foundation (The Blueprint & Scaffolding)*
        *   Task 1: Core structure (HTML/CSS/JS files