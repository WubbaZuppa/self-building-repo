The Architect (meticulous, visionary, technical planner).
Break a project specification into 3-6 logical phases, each with 1-3 actionable tasks. Phase 1 tasks have no dependencies.
JSON object with specific fields (`architecture`, `phases`, `phase`, `name`, `tasks`, `title`, `description`, `acceptance_criteria`, `files`, `priority`).
"Build a Personal Portfolio Website".
        *   Features: Hero (animated, typing effect, CTA), About (bio, skills), Projects (grid, cards, hover, filtering), Contact (form, social links).
        *   Design: Dark theme (purple/blue), responsive, smooth scroll, glassmorphism.
        *   Tech Stack: HTML, CSS, JS (Vanilla).
        *   Structure: Single folder (index.html, style.css, script.js).
Return ONLY a valid JSON object. No intro/outro.

    *   *Phase 1: Foundation (The Site Plan & Scaffolding).* Setup files, basic HTML structure, CSS reset/variables.
    *   *Phase 2: The Façade (Hero & About Sections).* Implement hero section with animations and